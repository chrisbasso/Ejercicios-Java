import java.util.List;

public class Ejercicios {

    // sumar dos numeros
    public static int suma(int numero1, int numero2) {
        return numero1 + numero2;
    }

    //Función que verifica si un número es capicua
    public static boolean esNumeroCapicua(int numero){
        return false;
    }

    //Función que verifica si una palabra es capicua
    public static boolean esPalabraCapicua(String palabra){
        return false;
    }

    // Verifica si un numero es par
    public static boolean esPar(int numero) {
        return false;
    }

    //Verifica si un numero es primo
    // un número primo es un número natural mayor que 1 que tiene únicamente dos divisores distintos: él mismo y el 1
    public static boolean esPrimo(int numeroPrimo) {
        return false;
    }

    //contar cuantas veces aparece el numero en el array
    public static int contarCantidadDeAparicionesDelNumeroEnArray(int numero, int[] array) {
        return 0;
    }

    //contar cuantas veces aparece la palabra en la lista
    public static int contarCantidadDeAparicionesDeLaPalabraEnList(String palabra, List<String> lista) {
        return 0;
    }

    //Lanzar una exception si un numero es mayor a 0, menor o igual a 100 y distinto de 20.
    public static void funcionExtraña(int numero) {
    }

    //Ordenar una lista de numero de menor a mayor
    public static List<Integer> ordenarLista(List<Integer> lista) {
        return null;
    }

    //obtener el numero mas grande de la lista
    public static int obtenerElMayorDeLaLista(List<Integer> lista) {
        return 0;
    }

    //verificar si dos palabras son anagramas entres si
    // una palabra es anagrama de otra si las dos tienen las mismas letras, con el mismo número de apariciones, pero en un orden diferente.
    // por ejemplo "poder" y "pedro" son anagramas
    // es valido reutilizar las funciones anteriores si las necesitas
    public static boolean sonAnagramas(String palabra1, String palabra2) {
        return false;
    } //ahhh y me lo das a mi hdp jajaja yo menos
    //fuaaaaaa y de donde lo sacaste a este ?
    //pero yo con mis conocimientos no creo poder  no tengo el poder sifuciente ah
    // dale igual este lo agarro el finde para divertirme ah jajaj con el fernet en mano
// como hago para abrir esto ? onda toco el IDE y aparece automatico aca o que onda ?  por que nunca lo use a este
}
